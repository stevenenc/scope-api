version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: scope-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - scope_mongo_data:/data/db

volumes:
  scope_mongo_data: